# NGLLinkSpammer
A simple ngl.link spammer written in Node.js. Should be used in repl.it with multiple instances, since this doesn't support proxies yet. however, captcha solving isn't supported yet, that will be implemented later.
